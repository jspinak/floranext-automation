rootProject.name = 'floranext'

// Include the local Brobot library project as a composite build
includeBuild('../brobot') {
    dependencySubstitution {
        substitute module('io.github.jspinak:brobot:1.1.0') using project(':library')
    }
}